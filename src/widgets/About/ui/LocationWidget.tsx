"use client";

import { Location } from "@entities/profiles";
import { Section } from "../style/common.styled";

const LocationWidget = () => {
  return (
    <Section>
      <Location />
    </Section>
  );
};

export default LocationWidget;
